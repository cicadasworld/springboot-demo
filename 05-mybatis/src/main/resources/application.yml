spring:
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3306/mybatis?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  type-aliases-package: com.jin.pojo
  mapper-locations:
    - classpath:mapper/*.xml